paths=( "submodules/ios-profile" "soomla-native/projects/cocos2dx-ios-profile" )
build=../build/ios

rm -rf $build
mkdir -p $build

for path in "${paths[@]}"
do
  cd $path
  ./build_all
  cd -
  cp -RL $path/build/* $build/
done
